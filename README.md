# DayNotes

A tiny offline weekly paper logbook. Notes are stored only in the browser's localStorage for that device. Use **Backup → Export JSON** to move or preserve them, and **Import / Restore JSON** to restore a backup on another device.
